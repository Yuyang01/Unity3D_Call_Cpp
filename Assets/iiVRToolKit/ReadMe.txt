iiVR_API package

dependencies :

For oculus management : use package OculusUtilities , add the preprocessor value define at the top of script oculusOffsetManager.cs

For vive management : use steamVr packages , add the preprocessor value define at the top of script vivePadSelector.cs and replaceRoomViveManager.cs